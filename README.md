# chatbot

To install dependencies (client + server):

```bash
bun install (root)
```

To run:

```bash
bun run dev
pay attention about the env.
```

This project was created using `bun init` in bun v1.3.3. [Bun](https://bun.com) is a fast all-in-one JavaScript runtime.
